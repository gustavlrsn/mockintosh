// const fs = require("fs");

// const copyFile = (path) => {
//   fs.copyFile(path, `./out/${path}`, (err) => {
//     if (err) throw err;
//     console.log(`${path} was copied to ./out/${path}`);
//   });
// };

// copyFile("README.md");
// copyFile("CONTRIBUTING.md");
// copyFile("todo.md");
